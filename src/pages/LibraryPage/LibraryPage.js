import classNames from "classnames/bind";
import styles from './LibraryPageMobile.module.scss';

const cx= classNames.bind(styles)
function LibraryPage() {
    return ( <div>
        Library Page
    </div> );
}

export default LibraryPage;